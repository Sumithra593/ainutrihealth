# Ingredient OCR & Allergen Detector — Frontend (Vite + React + Tailwind)

This frontend expects your Flask backend at http://localhost:5000 with a POST /predict endpoint that accepts multipart/form-data with field `file` and returns JSON.

Sample CSV (uploaded earlier) is referenced in the app at:
/mnt/data/nutriscan_1200_semi_realistic.csv

## Install & run

1. cd into this folder
2. npm install
3. npm run dev

